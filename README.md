# NPSP Public Donation & Payment Gateway Integration Engine

An enterprise-grade implementation showcasing modern Salesforce architecture layered over the **Nonprofit Success Pack (NPSP)** ecosystem.

## ⚙️ Quickstart Developer Setup

1. Authenticate the NPSP Developer Environment:
   ```bash
   sf org login web --alias npsp-dev --set-default
   ```
